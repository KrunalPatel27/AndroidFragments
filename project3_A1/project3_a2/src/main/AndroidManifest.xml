<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project3_a2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name=".Attractions"
            android:permission="com.example.krunal.project3_a1">
            <intent-filter>
                <action android:name="com.example.krunal.project3.attractions" />
            </intent-filter>
        </receiver>
        <receiver android:name=".Restaurants"
            android:permission="com.example.krunal.project3_a1">
            <intent-filter>
                <action android:name="com.example.krunal.project3.restaurants" />
            </intent-filter>
        </receiver>

        <activity android:name=".ActivityAttractions"></activity>
        <activity android:name=".ActivityRestaurants"></activity>
    </application>

</manifest>